I only wrote the following functions in tsh.cc: eval(), builtin_cmd(), do_bgfg(), waitfg(), sigchld_handler(),
sigint_handler(), and sigstp_handler().  All other code included is the work of others.  