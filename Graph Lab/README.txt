My code is only in graph.cpp and includes only these methods: ~graph(), add_vertex(), 
add_edge(), does_dfs_path_exist(), does_bfs_path_exist(), depth_first_search(), breadth_first_search(), 
and part of init_graph_from_file(). All other code is the work of others.  